# Dashboard Plugin Surface

This folder documents the public dashboard plugin surface.

The packaged runtime dashboard lives under:

```text
runtime/dashboard/
```

Public plugin boundaries:

- localhost-only dashboard
- no raw prompt display
- no private memory display
- allowlisted control commands only
- confirmation required for high-risk commands
