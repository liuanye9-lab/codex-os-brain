# Public Evals

This directory contains public smoke and regression eval descriptions for ACOB.

The public project keeps evals small and deterministic:

- hook installation smoke
- dispatch gate behavior
- privacy scan behavior
- dashboard control-plane allowlist behavior
- tool-call reliability smoke cases

Private task traces and user-specific evals are intentionally excluded.
