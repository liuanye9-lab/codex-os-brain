# OS-Agent Bridge

The private project contains a larger OS-Agent bridge. The public package exposes only the reusable harness boundary:

- local hook entry
- dispatch planning
- verification gates
- privacy scan
- dashboard control-plane allowlist

Full private automation state, local machines, private tunnels, logs, and personal runtime data are not included in this public repository.
